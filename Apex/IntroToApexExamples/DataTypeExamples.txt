public class DataTypesExamples {
    /*
// Boolean Example - can be set to true/false/null
Boolean isWeekend = false;

// Date Example - A value that indicates a particular day.
Date startOfTraining = Date.newInstance(2019, 2, 25);

// Use System.debug() to print to a debug log.
System.debug('startOfTraining: ' + startOfTraining.day());

// Decimal Example - A number that includes a decimal point.
// Currency fields are automatically assigned the type Decimal
Decimal cost = 19.99;
System.debug('cost: ' + cost);

// Double Example - A 64-bit number that includes a decimal point. 
Double costDouble = 19.99;
System.debug('costDouble: ' + costDouble);

// Decimal vs Double - You should NEVER use floating point representations (Double) to handle
// monetary calculations, instead use Decimal.
Double zeroPointZeroOne = 0.01;
Double doubleSum = 0.0;
for (Integer i = 0; i < 10; i++) {
doubleSum += zeroPointZeroOne;
}

System.debug('doubleSum = ' + doubleSum);

Decimal zeroPointZeroOneD = 0.01;
Decimal decimalSum = 0.0;
for (Integer i = 0; i < 10; i++) {
decimalSum += zeroPointZeroOneD;
}

System.debug('decimalSum = ' + decimalSum);

Integer Example - A 32-bit number that does not invlude a decimal point.
Minimum value = -2,147,483,648 & Maximum value = 2,147,483,647

Integer age = 23;

// Long Example - A 64-bit number that does not include a decimal point.
Long distanceInInchesToTheMoon = 15130000000L;

// Object Example - Any data type that is supported in Apex.
Object obj = 10;
Object obj2 = new Account(Name='Matt');
System.debug('obj: ' + obj);
System.debug('obj2: ' + obj2);

// String Example - Any set of characters surrounded by single quotes.
String sentence = 'The quick brown fox jumped over the lazy dog';

// sObject Example
Contact c = new Contact();
c.lastName = 'Ruiz';
System.debug('lastName: ' + c.lastName);

*/
    
    
    
    
}